# ============================================
# MLS Automation - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_DB=mls
POSTGRES_USER=mls
POSTGRES_PASSWORD=mls@123
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# ============================================
# STORAGE CONFIGURATION
# ============================================
# Root directory for storing uploaded documents and images
STORAGE_ROOT=storage

# ============================================
# GEMINI API (Required for AI features)
# ============================================
# Get your API key from: https://makersuite.google.com/app/apikey
# Used for: document extraction, image analysis, listing descriptions
GEMINI_API_KEY=

# Optional: Override default Gemini models
# VISION_MODEL=gemini-2.5-flash              # For document extraction (default: gemini-2.5-flash)
# IMAGE_VISION_MODEL=gemini-2.5-flash        # For image analysis (default: gemini-2.5-flash)
# LLM_MODEL=gemini-2.5-flash                 # For text generation (default: gemini-2.5-flash)

# Backward compatibility aliases (optional - falls back to GEMINI_API_KEY)
# VISION_API_KEY=                              # Falls back to GEMINI_API_KEY
# LLM_API_KEY=                                # Falls back to GEMINI_API_KEY

# ============================================
# GOOGLE MAPS API (Required for geo-intelligence enrichment)
# ============================================
# Get your API key from: https://console.cloud.google.com/
# Required APIs to enable:
#   - Geocoding API
#   - Places API
#   - Directions API
#   - Roads API (optional, for better road detection)
# Used for: location enrichment, nearby POIs, directions, water body detection
GOOGLE_MAPS_API_KEY=

# ============================================
# POPPLER (Optional - for PDF to image conversion)
# ============================================
# Only needed if Poppler is not in your system PATH
# Windows example:
# POPPLER_PATH=C:\Program Files\poppler\Library\bin
# Linux/Mac: Usually not needed if installed via package manager

# ============================================
# GROQ API (Optional - for alternative LLM in native text extraction)
# ============================================
# Used as fallback in extraction_native_text.py
# GROQ_API_KEY=